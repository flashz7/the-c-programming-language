I would test the word count program by trying each of the use cases (a newline, a word and a char). I would then try to use numbers, or words that would exceed the int char buffer to see how the program handled it.